# length-and-width-manually
c-9
#include<stdio.h>
int main(){
    float l,w,a;
    printf("Enter the length:");
    scanf("%f",&l);
    printf("Enter the width:");
    scanf("%f",&w);
    a=1*w;
    printf("Area of a rectangle =%.4f square units \n,a");
    return 0;
}
